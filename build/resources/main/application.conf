ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.chargepoint.async-charging.ApplicationKt.module ]
    }
}

authorization {
    timeoutMillis = 30000
}

callback {
    timeoutMillis = 10000
}

logging {
    level = INFO
}
